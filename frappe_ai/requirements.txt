# Gemini SDK — primary LLM provider (free tier: gemini-2.0-flash)
google-genai>=1.0.0,<2.0.0

# Token counting fallback for providers without a native counter
tiktoken>=0.7.0,<1.0.0

# Async HTTP client — used by future OpenAI/Anthropic provider implementations
httpx>=0.27.0,<1.0.0
